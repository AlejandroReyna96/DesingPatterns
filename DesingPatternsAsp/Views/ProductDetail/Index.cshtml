@{ 
    decimal totalLocal = (decimal)ViewBag.totalLocal;
    decimal totalForeing = (decimal)ViewBag.totalForeing;
}

<div class="row">
    <div class="col-12">
        <label>Total local:</label>
        <label>@totalLocal</label>
        <hr />
        <label>Total local:</label>
        <label>@totalForeing</label>
    </div>
</div>